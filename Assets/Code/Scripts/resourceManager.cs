using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class resourceManager : MonoBehaviour
{
    /*//[SerializeField]
    public TextMeshProUGUI AmntStone;
    //[SerializeField]
    public TextMeshProUGUI AmntWood;
    //[SerializeField]
    public TextMeshProUGUI AmntGrave;

    private int initialStone = 0;
    private int initialWood = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void stoneUp()
    {
        AmntStone.text = "Stone: " + initialStone++;
    }
    public void woodUp()
    {
        AmntWood.text = "Wood: " + initialWood++;
    }*/
}
